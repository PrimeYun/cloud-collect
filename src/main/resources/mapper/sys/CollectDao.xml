<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.collect.modules.sys.dao.CollectDao">
	
	<select id="selectList" resultType="Collect">
		SELECT 
			id,
			name,
			url,
			source,
			create_date,
			remarks
		FROM
			collect
		WHERE
			status = 0
		ORDER BY id
	</select>
	
	<insert id="insertCollect">
		INSERT INTO	collect(id, name, url, source, create_date, remarks, status)
		VALUES(#{id}, #{name}, #{url}, #{source}, #{createDate}, #{remarks}, 0)
	</insert>
</mapper>